# Copy to /etc/spotifydisplay.env and fill in.

# Required: per-device secret that authenticates this Pi to the token broker.
# Must match the value stored at SSM /website/spotify/device-secret.
SPOTIFY_DEVICE_TOKEN=

# Optional: override the broker token endpoint.
#SPOTIFY_TOKEN_URL=https://api.maxrosoff.com/spotify/token
